version: '3.8'

services:
  app:
    build: ./app
    ports:
      - "3000:3000"
    env_file:
      - ./app/.env
    depends_on:
      - redis
    restart: always

  redis:
    image: redis:alpine
    ports:
      - "6380:6380"
    command: redis-server --port 6380
    restart: always
