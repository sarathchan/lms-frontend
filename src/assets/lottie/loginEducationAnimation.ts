import loginEducation from './login-education.json'

/**
 * Student at a desk with laptop, notebook, and browser window — fits login / e-learning.
 * Derived from a LottieFiles community package (`lf20_4kx2q32n.json`); confirm
 * LottieFiles license terms if you ship commercially.
 */
export const loginEducationAnimation = loginEducation as Record<string, unknown>
